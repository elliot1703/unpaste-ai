# react-helmet-async@2 declares peer react <=18; we run React 19 (works fine in
# practice, helmet just hasn't updated its range). Without this flag every
# `npm install` of a new package fails on that stale peer range.
legacy-peer-deps=true
